// import createHttpError from 'http-errors';

// export const auditAdminMiddleware = (req, res, next) => {
//   const role = req.user.role;

//   if (role !== 'admin') {
//     throw createHttpError(403, 'No access.');
//   }

//   next();
// };
